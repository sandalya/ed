# Ed — конфіг цільових ботів
# reset_command виконується перед кейсом з "reset_before": true
# Використовуй для ботів з persistent state (handoff_state.json, sqlite, тощо)

abby:
  username: "@abby_ksu_bot"
  # Abby не має persistent state який треба скидати між тестами
  reset_command: null

insilver:
  username: "@insilver_v3_bot"
  reset_command: "echo '{}' > /home/sashok/.openclaw/workspace/insilver-v3/data/handoff_state.json"

garcia:
  username: "@garcia_ksu_bot"
  reset_command: null
sam:
  username: "@sashoks_assistant1_sam_bot"
  # Sam має learning_state.json, але для QA не скидаємо — тести орієнтовані на існуючі теми
  reset_command: null
